import type {NextApiRequest,NextApiResponse} from 'next';import {createClient}from '@supabase/supabase-js';export default async function h(req:NextApiRequest,res:NextApiResponse){if(req.method!=='POST')return res.status(405).send('method not allowed');const {recording_id,user_id,access_start,access_end}=req.body||{};if(!recording_id||!user_id||!access_start||!access_end)return res.status(400).send('missing fields');const s=createClient(process.env.SUPABASE_URL!,process.env.SUPABASE_SERVICE_KEY!);const {error}=await s.from('vod_access').insert({recording_id,user_id,access_start,access_end} as any);if(error)return res.status(400).send(error.message);return res.status(200).send('granted');}