import type {NextApiRequest,NextApiResponse} from 'next';import {createClient} from '@supabase/supabase-js';export default async function h(req:NextApiRequest,res:NextApiResponse){const s=createClient(process.env.SUPABASE_URL!,process.env.SUPABASE_SERVICE_KEY!);const {sessionId,output}=req.body||{};if(!sessionId||!output?.file?.path)return res.status(200).send('ignored');const {error}=await s.from('recordings').insert({live_session_id:sessionId,storage_path:output.file.path,duration_seconds:output.file.duration||null} as any);if(error)return res.status(400).send(error.message);return res.status(200).send('ok');}